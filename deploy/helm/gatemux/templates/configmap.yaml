apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gatemux.fullname" . }}-config
  labels:
    {{- include "gatemux.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
