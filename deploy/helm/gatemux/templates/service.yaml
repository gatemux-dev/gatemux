apiVersion: v1
kind: Service
metadata:
  name: {{ include "gatemux.fullname" . }}
  labels:
    {{- include "gatemux.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gatemux.selectorLabels" . | nindent 4 }}
